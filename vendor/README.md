# Vendor

This directory contains third-party libraries and dependencies used by the project. These files are included to ensure that the project can be built and run without requiring external downloads or installations.
